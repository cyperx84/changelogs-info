---
import ClaudeCodeLayout from "../../layouts/ClaudeCodeLayout.astro";
import { getToolReleases, formatDate } from "../../lib/releases";
import { marked } from "marked";

const data = await getToolReleases("claude-code");
const releases = data?.releases || [];

function isBreaking(body: string = ""): boolean {
  return body.toLowerCase().includes("breaking");
}

function renderBody(body: string): string {
  if (!body) return "";
  marked.setOptions({ gfm: true, breaks: false });
  let html = marked.parse(body) as string;
  html = html.replace(/<a href="(https:\/\/github\.com\/[^/]+\/[^/]+\/pull\/(\d+))"[^>]*>[^<]*<\/a>/g,
    '<a href="$1" target="_blank" rel="noopener noreferrer" class="cl-pr-link">#$2</a>');
  html = html.replace(/<a href="(https:\/\/github\.com\/[^/]+\/[^/]+\/issues\/(\d+))"[^>]*>[^<]*<\/a>/g,
    '<a href="$1" target="_blank" rel="noopener noreferrer" class="cl-pr-link">#$2</a>');
  html = html.replace(/ by <a href="https:\/\/github\.com\/[^"]*"[^>]*>@[^<]+<\/a> in /g, ' ');
  html = html.replace(/ by @[\w-]+ in /g, ' ');
  html = html.replace(/<li>(.*?)<\/li>/gs, (match, content) => {
    const text = content.toLowerCase();
    let badge = '';
    if (/^fix[\s(:]/.test(text)) badge = '<span class="cl-badge cl-badge-fix">FIX</span>';
    else if (/^feat[\s(:]/.test(text)) badge = '<span class="cl-badge cl-badge-feat">FEAT</span>';
    else if (/^docs[\s(:]/.test(text)) badge = '<span class="cl-badge cl-badge-docs">DOCS</span>';
    else if (/^test[\s(:]/.test(text)) badge = '<span class="cl-badge cl-badge-test">TEST</span>';
    else if (/^(refactor|chore)[\s(:]/.test(text)) badge = '<span class="cl-badge cl-badge-chore">CHORE</span>';
    else if (/^(ci|build|docker)[\s(:]/.test(text)) badge = '<span class="cl-badge cl-badge-ci">CI</span>';
    else if (/^(perf|style)[\s(:]/.test(text)) badge = '<span class="cl-badge cl-badge-perf">PERF</span>';
    return `<li>${badge}${content}</li>`;
  });
  return html;
}
---

<ClaudeCodeLayout
  title="Claude Code Changelog — changelogs.info"
  description="Full changelog for Claude Code. Every release, every breaking change, every fix."
  currentPage="changelog"
>
  <div style="max-width: 1280px; margin: 0 auto; padding: 2rem 1.5rem 0;">

    <!-- Hero -->
    <section style="margin-bottom: 4rem;">
      <h1
        class="cc-headline"
        style="font-size: clamp(3.5rem, 10vw, 7rem); font-weight: 900; text-transform: uppercase; font-style: italic; letter-spacing: -0.03em; line-height: 0.9; margin: 0 0 1.5rem; color: var(--cc-text);"
      >
        Changelog
      </h1>
      <div style="display: flex; flex-wrap: wrap; gap: 2rem; align-items: flex-end; justify-content: space-between;">
        <p style="font-family: var(--cc-font-body); font-size: 1.1rem; max-width: 32rem; color: var(--cc-text-dim); line-height: 1.6; margin: 0; text-transform: uppercase; letter-spacing: 0.05em;">
          Every Claude Code release. Bolted straight to the record.
        </p>
        <!-- Search + filters -->
        <div style="width: 100%; max-width: 320px;">
          <label class="cc-headline" style="display: block; font-size: 0.65rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.15em; color: var(--cc-primary-bright); margin-bottom: 0.5rem;">
            Filter updates
          </label>
          <div style="position: relative;">
            <input
              id="cl-search"
              type="text"
              placeholder="SEARCH RELEASES..."
              style="width: 100%; background: var(--cc-bg); border: 4px solid var(--cc-border); padding: 1rem; font-family: var(--cc-font-headline); font-weight: 700; text-transform: uppercase; color: var(--cc-text); font-size: 0.85rem; outline: none; box-shadow: var(--cc-shadow); transition: border-color 100ms;"
              onfocus="this.style.borderColor='var(--cc-primary-bright)'"
              onblur="this.style.borderColor='var(--cc-border)'"
            />
            <span style="position: absolute; right: 1rem; top: 50%; transform: translateY(-50%); color: var(--cc-text-muted); font-size: 1rem;">⌕</span>
          </div>
          <div style="display: flex; gap: 0.5rem; margin-top: 0.75rem;">
            {["stable", "breaking", "prerelease"].map((f) => (
              <button
                id={`cl-filter-${f}`}
                class="cc-headline"
                data-filter={f}
                style="background: transparent; border: 2px solid var(--cc-border-dim); padding: 0.3rem 0.75rem; font-size: 0.65rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--cc-text-muted); cursor: pointer; transition: all 100ms;"
              >
                {f === "prerelease" ? "Pre-release" : f.charAt(0).toUpperCase() + f.slice(1)}
              </button>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- Timeline -->
    {releases.length > 0 ? (
      <div
        id="cl-releases-list"
        style="position: relative; padding-left: 3rem; border-left: 6px solid var(--cc-border);"
      >
        {releases.map((release, idx) => {
          const breaking = isBreaking(release.body);
          const renderedBody = renderBody(release.body || "");
          const isLong = (release.body || "").length > 600;

          return (
            <article
              class="cl-entry"
              data-version={release.version.toLowerCase()}
              data-name={(release.name || "").toLowerCase()}
              data-body={(release.body || "").toLowerCase().slice(0, 800)}
              data-prerelease={String(release.prerelease)}
              data-breaking={String(breaking)}
              data-index={idx}
              style={`position: relative; margin-bottom: 5rem; ${idx >= 10 ? 'display: none;' : ''}`}
            >
              <!-- Timeline dot -->
              <div
                style={`position: absolute; left: -3.9rem; top: 0.5rem; width: 3rem; height: 3rem; border: 4px solid var(--cc-border); display: flex; align-items: center; justify-content: center; background: ${breaking ? 'var(--cc-primary)' : 'var(--cc-bg-card)'};`}
              >
                <span style={`font-size: 0.8rem; color: ${breaking ? 'white' : 'var(--cc-primary-bright)'};`}>
                  {breaking ? "⚡" : release.prerelease ? "β" : "●"}
                </span>
              </div>

              <!-- Entry header -->
              <div style="display: flex; flex-wrap: wrap; gap: 1rem; align-items: baseline; margin-bottom: 1rem;">
                <span class="cc-headline" style="font-size: 2.25rem; font-weight: 900; font-style: italic; letter-spacing: -0.02em; color: var(--cc-text);">
                  {release.version}
                </span>
                <span class="cc-headline" style="font-size: 1rem; font-weight: 700; text-transform: uppercase; color: var(--cc-text-muted); letter-spacing: 0.05em;">
                  {formatDate(release.date)}
                </span>
                {breaking && (
                  <span style="background: var(--cc-primary); color: white; padding: 0.2rem 0.75rem; font-family: var(--cc-font-headline); font-size: 0.7rem; font-weight: 900; text-transform: uppercase; letter-spacing: 0.1em; border: 2px solid var(--cc-border);">
                    BREAKING
                  </span>
                )}
                {release.prerelease && (
                  <span style="background: rgba(245,158,11,0.15); color: #f59e0b; padding: 0.2rem 0.75rem; font-family: var(--cc-font-headline); font-size: 0.7rem; font-weight: 900; text-transform: uppercase; letter-spacing: 0.1em; border: 2px solid #f59e0b;">
                    PRE-RELEASE
                  </span>
                )}
              </div>

              <!-- Entry card -->
              <div style={`padding: 2rem; border: 4px solid var(--cc-border); background: ${breaking ? 'rgba(245,158,11,0.08)' : 'var(--cc-bg-surface)'}; ${breaking ? 'border-left: 8px solid var(--cc-primary);' : ''}`}>
                <h3 class="cc-headline" style="font-size: 1.5rem; font-weight: 900; text-transform: uppercase; margin: 0 0 1.5rem; letter-spacing: -0.01em; color: var(--cc-text);">
                  {(release.name || release.version).replace(/^claude[\s-]code\s+/i, "")}
                </h3>

                <div
                  class={`changelog-body ${isLong && idx > 0 ? 'cl-collapsed' : ''}`}
                  style="color: var(--cc-text-dim); font-size: 0.875rem; line-height: 1.7; font-family: var(--cc-font-body); overflow: hidden; position: relative;"
                  set:html={renderedBody}
                />

                {isLong && (
                  <button
                    class="cl-expand-btn cc-headline"
                    style="display: block; margin-top: 1rem; background: transparent; border: 2px solid var(--cc-border); color: var(--cc-text); padding: 0.5rem 1.25rem; font-size: 0.7rem; font-weight: 900; text-transform: uppercase; letter-spacing: 0.1em; cursor: pointer; transition: background 100ms;"
                    onclick="this.previousElementSibling.classList.toggle('cl-collapsed'); this.textContent = this.textContent.includes('EXPAND') ? '▲ COLLAPSE' : '▼ EXPAND FULL CHANGELOG'"
                  >
                    {idx === 0 ? '▲ COLLAPSE' : '▼ EXPAND FULL CHANGELOG'}
                  </button>
                )}

                <div style="display: flex; justify-content: flex-end; margin-top: 1rem; padding-top: 0.75rem; border-top: 1px solid var(--cc-border-dim);">
                  <a
                    href={release.url}
                    target="_blank"
                    rel="noopener noreferrer"
                    style="font-family: var(--cc-font-mono); font-size: 0.65rem; color: var(--cc-text-muted); text-decoration: none; text-transform: uppercase; letter-spacing: 0.1em; transition: color 150ms;"
                    onmouseover="this.style.color='var(--cc-primary-bright)'"
                    onmouseout="this.style.color='var(--cc-text-muted)'"
                  >
                    ↗ GitHub
                  </a>
                </div>
              </div>
            </article>
          );
        })}

        <!-- Load more -->
        {releases.length > 10 && (
          <div style="padding-left: 0; margin-bottom: 4rem;">
            <button
              id="cl-load-more"
              class="cc-headline"
              style="background: transparent; border: 4px solid var(--cc-border); color: var(--cc-text); padding: 1rem 2.5rem; font-size: 0.85rem; font-weight: 900; text-transform: uppercase; letter-spacing: 0.1em; cursor: pointer; transition: background 100ms, color 100ms; font-style: italic;"
              onmouseover="this.style.background='var(--cc-primary)';this.style.color='white'"
              onmouseout="this.style.background='transparent';this.style.color='var(--cc-text)'"
            >
              Load {releases.length - 10} More Releases ▼
            </button>
          </div>
        )}
      </div>
    ) : (
      <div style="padding: 4rem 2rem; border: 4px dashed var(--cc-border-dim); text-align: center;">
        <p style="font-family: var(--cc-font-mono); color: var(--cc-text-muted);">$ no releases tracked yet</p>
      </div>
    )}

    <!-- Scroll to top -->
    <button
      id="cl-scroll-top"
      style="position: fixed; bottom: 2rem; right: 2rem; background: var(--cc-primary); color: white; border: 4px solid var(--cc-border); padding: 0.75rem; cursor: pointer; font-size: 1rem; box-shadow: var(--cc-shadow); opacity: 0; pointer-events: none; transition: opacity 200ms; z-index: 50;"
      onclick="window.scrollTo({top: 0, behavior: 'smooth'})"
      aria-label="Scroll to top"
    >
      ↑
    </button>
  </div>

  <script>
    const entries = Array.from(document.querySelectorAll('.cl-entry')) as HTMLElement[];
    const searchInput = document.getElementById('cl-search') as HTMLInputElement;
    const stableBtn = document.getElementById('cl-filter-stable');
    const breakingBtn = document.getElementById('cl-filter-breaking');
    const prereleaseBtn = document.getElementById('cl-filter-prerelease');
    const scrollTopBtn = document.getElementById('cl-scroll-top');

    let activeFilter = '';
    let searchQuery = '';
    let allLoaded = entries.length <= 10;

    function filterEntries() {
      entries.forEach((entry, idx) => {
        const version = entry.dataset.version || '';
        const name = entry.dataset.name || '';
        const body = entry.dataset.body || '';
        const prerelease = entry.dataset.prerelease === 'true';
        const breaking = entry.dataset.breaking === 'true';

        const matchesSearch = !searchQuery ||
          version.includes(searchQuery) ||
          name.includes(searchQuery) ||
          body.includes(searchQuery);

        const matchesFilter =
          activeFilter === '' ||
          (activeFilter === 'stable' && !prerelease) ||
          (activeFilter === 'breaking' && breaking) ||
          (activeFilter === 'prerelease' && prerelease);

        const withinLoadLimit = allLoaded || idx < 10;

        entry.style.display = (matchesSearch && matchesFilter && withinLoadLimit) ? '' : 'none';
      });
    }

    searchInput?.addEventListener('input', () => {
      searchQuery = searchInput.value.toLowerCase().trim();
      if (searchQuery) allLoaded = true;
      filterEntries();
    });

    function resetFilterBtns() {
      [stableBtn, breakingBtn, prereleaseBtn].forEach(btn => {
        if (btn) {
          (btn as HTMLElement).style.background = 'transparent';
          (btn as HTMLElement).style.borderColor = 'var(--cc-border-dim)';
          (btn as HTMLElement).style.color = 'var(--cc-text-muted)';
        }
      });
    }

    function activateBtn(btn: HTMLElement | null) {
      if (btn) {
        (btn as HTMLElement).style.background = 'var(--cc-primary)';
        (btn as HTMLElement).style.borderColor = 'var(--cc-primary)';
        (btn as HTMLElement).style.color = 'white';
      }
    }

    [stableBtn, breakingBtn, prereleaseBtn].forEach((btn) => {
      btn?.addEventListener('click', () => {
        const f = (btn as HTMLElement).getAttribute('data-filter') || '';
        if (activeFilter === f) {
          activeFilter = '';
          resetFilterBtns();
        } else {
          activeFilter = f;
          resetFilterBtns();
          activateBtn(btn as HTMLElement);
        }
        filterEntries();
      });
    });

    const loadMoreBtn = document.getElementById('cl-load-more');
    loadMoreBtn?.addEventListener('click', () => {
      allLoaded = true;
      if (loadMoreBtn) loadMoreBtn.style.display = 'none';
      filterEntries();
    });

    window.addEventListener('scroll', () => {
      if (scrollTopBtn) {
        scrollTopBtn.style.opacity = window.scrollY > 400 ? '1' : '0';
        scrollTopBtn.style.pointerEvents = window.scrollY > 400 ? 'auto' : 'none';
      }
    }, { passive: true });
  </script>
</ClaudeCodeLayout>
